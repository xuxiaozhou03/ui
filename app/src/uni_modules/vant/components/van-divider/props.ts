import { commonProps } from "../../utils";

export const dividerProps = {
  ...commonProps,
  dashed: Boolean,
  hairline: Boolean,
  contentPosition: String,
  fontSize: String,
  borderColor: String,
  textColor: String,
};
